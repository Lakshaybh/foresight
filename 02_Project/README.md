# 02_Project

This folder will hold the actual Foresight application — frontend, backend, everything
that gets built and run.

It is intentionally empty right now. We are in the pre-development phase (see
`CLAUDE.md` at the repo root). Nothing gets scaffolded here — no Next.js app, no
FastAPI service, no dependencies — until development is explicitly authorized.
