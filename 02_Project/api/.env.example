SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Added later, once direct database access is needed. Never commit a real value.
SUPABASE_SERVICE_ROLE_KEY=
# Supabase dashboard -> Project Settings -> Database -> Connection string (URI).
# Required to run the ingestion scripts in scripts/. Never commit a real value.
DATABASE_URL=
